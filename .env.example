# Copy to .env and fill in real values. Never commit the real .env — see .gitignore.
# These are read by the Electron main process only (non-negotiable #6 in CLAUDE.md).

# NASA DONKI — CME/flare data with predicted Earth-impact timing. Free key: api.nasa.gov
NASA_DONKI_API_KEY=
